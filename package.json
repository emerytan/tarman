{
  "name": "gc-rtr",
  "version": "1.0.0",
  "description": "LTFS Duplicator",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/electron main.js",
    "watch": "rollup -c --watch",
    "build": "mkdir -p build && rollup -c"
  },
  "author": "Emery Anderson",
  "license": "MIT",
  "devDependencies": {
    "electron": "^3.0.8"
  },
  "dependencies": {
    "bootstrap": "^4.1.1",
    "popper.js": "^1.14.4"
  }
}
